{
  "name": "todo-app",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "npx json-server --watch db.json --port 3000",
    "start:bun": "bunx json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "tsx": "latest",
    "json-server": "latest"
  }
}
